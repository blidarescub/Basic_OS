# Physical memory size (in megabytes).
megs: 64

# CPU configs (don't change!).
cpu: count=1, ips=50000000

# CD-ROM image.
ata0: enabled=1, ioaddr1=0x1f0, ioaddr2=0x3f0, irq=14
ata0-master: type=cdrom, path="disk/myos.iso", status=inserted
boot: cdrom

# Don't change.
display_library: sdl
